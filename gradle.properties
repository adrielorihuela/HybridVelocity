group=com.velocitypowered
version=4.1.0-SNAPSHOT

# Name and version of this fork. These only affect the distributable proxy JAR and the
# version the proxy reports at runtime; `version` above stays on the upstream Velocity API
# version so that plugins built against velocity-api keep resolving unchanged.
forkName=HybridVelocity
forkVersion=1.1.0
