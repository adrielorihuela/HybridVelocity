# Discarded documents

Analysis that was written, considered and then superseded. It is kept because the reasoning is
still useful — mostly as a record of options that were evaluated and rejected, so they do not get
proposed again — but **nothing here is a plan to follow**.

The live documentation is in [../README.md](../README.md).

| Document | Why it was discarded |
| --- | --- |
| [offline-auth-roadmap.md](offline-auth-roadmap.md) | Weighed a from-scratch in-proxy limbo against a separate limbo server process. Both were ruled out by a later constraint: no extra backend to configure, and minimum RAM on a single host. Superseded by [../offline-auth-plan.md](../offline-auth-plan.md). Its "What both routes must do" section is still accurate and is referenced by the live plan. |
